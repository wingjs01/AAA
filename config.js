/* =========================================================
   部署設定 —— 只有這個檔案需要依你的環境修改
   ========================================================= */

/** 後端 Worker 的網址。留空則只有本機字庫可用，遊戲照常運作。 */
var API_BASE = '';        // 例：https://hangman-line-bot.your-name.workers.dev

/** LIFF ID。填了之後在 LINE 裡開啟會自動辨識身分；留空則只用專屬連結。 */
var LIFF_ID = '';         // 例：2000000000-abcdefgh
